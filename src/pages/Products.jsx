import {useState} from 'react';import products from '../data/product';import ProductCard from '../components/ProductCard';export default function Products({setCart}){const [search,setSearch]=useState('');const filtered=products.filter(p=>p.name.toLowerCase().includes(search.toLowerCase()));return <main className="products-page"><section className="products-header"><p>Fresh & Healthy</p><h1>Fresh Vegetables</h1><p>Choose fresh vegetables for your daily needs.</p><input placeholder="Search vegetables..." value={search} onChange={e=>setSearch(e.target.value)}/></section><div className="products-grid">{filtered.map(p=><ProductCard key={p.id} product={p} setCart={setCart}/>)}</div>{!filtered.length&&<p>No vegetables found.</p>}</main>}