import {useState} from 'react';import products from '../data/product';import ProductCard from '../components/ProductCard';import SearchBar from '../components/SearchBar';import './Products.css';export default function Products({setCart}){const [search,setSearch]=useState('');const filteredProducts=products.filter(p=>p.name.toLowerCase().includes(search.toLowerCase()));return <main className="products-page"><section className="products-header"><div><p className="products-eyebrow">Fresh & Healthy</p><h1>Fresh Vegetables</h1><p>Choose fresh vegetables for your daily needs.</p></div></section><SearchBar search={search} setSearch={setSearch}/><div className="products-grid">{filteredProducts.map(product=><ProductCard key={product.id} product={product} setCart={setCart}/>)}</div>{filteredProducts.length===0&&<p className="no-products">No vegetables found.</p>}</main>}