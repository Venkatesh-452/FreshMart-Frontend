import {useState} from 'react';import {Link} from 'react-router-dom';import initial from '../data/product';import './ManageProducts.css'
export default function ManageProducts(){const [products,setProducts]=useState(initial);const [search,setSearch]=useState('');const filtered=products.filter(p=>p.name.toLowerCase().includes(search.toLowerCase()));return <div className="manage-products"><div className="products-heading"><div><p className="products-eyebrow">Inventory</p><h2>Manage Products</h2><p>View and manage all FreshMart products.</p></div><Link className="add-product-button" to="/admin/products/add">+ Add Product</Link></div><section className="product-summary-cards"><div className="product-summary-card"><span className="product-summary-icon">🥕</span><div><small>Total Products</small><strong>{products.length}</strong></div></div><div className="product-summary-card"><span className="product-summary-icon">✓</span><div><small>Available</small><strong>{products.length}</strong></div></div></section><section className="products-card"><div className="products-toolbar"><div className="products-search-wrapper"><span>🔍</span><input placeholder="Search products..." value={search} onChange={e=>setSearch(e.target.value)}/></div></div><div className="products-table-wrapper"><table className="products-table"><thead><tr><th>Product</th><th>Category</th><th>Price</th><th>Actions</th></tr></thead><tbody>{filtered.map(p=><tr key={p.id}><td><div className="product-info-cell"><span className="product-image-placeholder">{p.image}</span><div><strong>{p.name}</strong><small>{p.unit}</small></div></div></td><td>{p.category}</td><td>₹{p.price}</td><td><div className="product-actions"><button className="product-delete-button" onClick={()=>setProducts(c=>c.filter(x=>x.id!==p.id))}>Delete</button></div></td></tr>)}</tbody></table></div></section></div>}