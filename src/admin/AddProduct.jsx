import {useState} from 'react';import {useNavigate} from 'react-router-dom';export default function AddProduct(){const nav=useNavigate();const [form,setForm]=useState({name:'',category:'Fresh Vegetables',price:'',unit:'kg',stock:'',description:''});const submit=e=>{e.preventDefault();if(!form.name||!form.price||!form.stock)return alert('Please enter product name, price and stock.');alert('Product added successfully!');nav('/admin/products')};return <form className="admin-card" onSubmit={submit}><h2>Add New Product</h2>{Object.keys(form).map(k=>k==='category'||k==='unit'?null:<input key={k} placeholder={k} value={form[k]} onChange={e=>setForm({...form,[k]:e.target.value})}/>)}<select value={form.category} onChange={e=>setForm({...form,category:e.target.value})}><option>Fresh Vegetables</option><option>Leafy Vegetables</option><option>Root Vegetables</option><option>Organic</option></select><button>Save Product</button></form>}